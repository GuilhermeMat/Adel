import { Box, Typography } from "@mui/material";

export default function Profile() {
    return (
        <Box
            className="pageContainer"
        >
            <Typography variant='h2' color='white'>Página do Perfil</Typography>
    
        </Box>
      );
}